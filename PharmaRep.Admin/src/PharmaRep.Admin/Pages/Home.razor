@page "/"
@inject NavigationManager NavManager

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();

        NavManager.NavigateTo(uri: "/users", replace: true);
    }
}
